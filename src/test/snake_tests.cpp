#include "snake_tests.hpp"

void test_snake_details() {
//	std::vector<sf::Vector2i> snake;
	// fill snake
//	auto details = get_snake_details(snake);
	
//	for (auto it = details.begin(); it != details.end(); ++it) {
//		PRINTLN(it->first.x << "," << it->first.y << ":" << it->second);
//	}
}